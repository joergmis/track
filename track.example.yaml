clockodo:
  email: your@email.ch          # user account for clockodo
  token: your-api-token         # can be found in the clockodo settings
storage:
  dir: track                    # folder will be appended to $HOME/.config/<your-dir>
